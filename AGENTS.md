# 项目协作约定

- 永远使用中文编写面向维护者和用户的项目文档。
- JavaScript/TypeScript 依赖与命令统一使用 Bun。
- Python 依赖与命令统一使用 uv。
- 先理解协议、状态所有权和安全边界，再做最小必要修改。
- 不覆盖用户改动，不提交秘密、本机状态或官方 bundle。
- 修改后运行 `bun run check`；无法验证时必须说明原因。
- 每个阶段结束后同步更新相关文档，并检查 Git 状态与提交范围。
